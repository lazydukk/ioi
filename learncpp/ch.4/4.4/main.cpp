short s; // prefer "short" instead of "short int"
int i;
long l;       // prefer "long" instead of "long int"
long long ll; // prefer "long long" instead of "long long int"

// integer types can also take an optional "signed" keyword
signed short ss;
signed int si;
signed long sl;
signed long long sll;
// redundant as integers are signed by default

int main() {
  return 0;
}
